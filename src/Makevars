CXX_STD=CXX17
PKG_LIBS=$(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e 'cat(tiledb::.pkg_config("PKG_CXX_LIBS"))') 
PKG_CPPFLAGS=$(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e 'cat(tiledb::.pkg_config("PKG_CXX_FLAGS"))') 
